import 'package:flutter/material.dart';

class AppLoginScreen extends StatelessWidget {
  const AppLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("App Logins"),
        centerTitle: true,
      ),
    );
  }
}
